const sizes = ["XS", "S", "M", "L", "XL", "XXL"];
import { antman, Avenger } from "./classes/basica";

console.log(Avenger.getAvgAge());
console.log(antman);

function App() {
  return (
    <div className="App">
      <h1>CALCULATOR</h1>
    </div>
  );
}

export default App;
